APP=dockerizednode
CUSER=${GITUSER}
KEYNAME=${GITKEYNAME}
KEYPATH=${GITKEYPATH}
OS=ubuntu
LOCALUSER="--arg=LOCALUSER=${USER}"
_USER_="-u ${CUSER}"
RUNENV="-e='DOCKER_ENV=dockerizednode'"
REMOVE=false
PRIV=--priv
