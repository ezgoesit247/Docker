APP=dockerizednode
PURPOSE=run
CUSER=${GITUSER}
KEYNAME=${GITKEYNAME}
KEYPATH=${GITKEYPATH}
OS=ubuntu
LOCALUSER="--arg=LOCALUSER=${USER}"
_USER_="-u ${CUSER}"
RUNENV="-e='DOCKER_ENV=dockerizednode'"
REMOVE=false
PRIV=true
RUNSTATE="-I -d"
